export default function spinnerLoader() {
    return (
        <div
            className="flex w-10 h-10 border-4 border-t-blue-500 border-gray-300 rounded-full animate-spin items-center justify-center"
        ></div>

    )
}